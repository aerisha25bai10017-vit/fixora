SECRET_KEY=change-this-to-a-random-string
JWT_SECRET_KEY=change-this-to-another-random-string
SLA_HOURS=24
# DATABASE_URL is optional - if unset, defaults to sqlite:///<project>/database/app.db
# DATABASE_URL=sqlite:///database/app.db

# Super Admin — seeded automatically on first run (see app.py / seed_superadmin.py).
# Super admin accounts can never be created through /register, only here.
SUPERADMIN_NAME=Super Admin
SUPERADMIN_EMAIL=superadmin@fixora.edu
SUPERADMIN_PASSWORD=ChangeMe@123
